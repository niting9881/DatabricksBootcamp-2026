# Databricks Apps deployment configuration
# Source code path: /Workspace/Users/<user>/Homework-3/mcp_server
# Deploy via: Apps V2 page or databricks apps deploy

command: python weather_mcp_server.py

env:
  - name: LOG_LEVEL
    value: INFO
  - name: MLFLOW_EXPERIMENT_NAME
    value: /Shared/weather-mcp-tracing
  # Databricks secret reference example (if using a paid weather API in future):
  # - name: WEATHER_API_KEY
  #   valueFrom:
  #     secretRef:
  #       key: api_key
  #       scope: weather
