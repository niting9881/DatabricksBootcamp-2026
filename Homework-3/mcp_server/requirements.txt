# Weather MCP Server — Python dependencies
# Tested with Python 3.10+

# MCP server framework (latest FastMCP 2.x)
fastmcp>=2.0.0

# HTTP client for Open-Meteo API calls
httpx>=0.27.0

# Data validation
pydantic>=2.0.0

# Environment variable loading (local dev)
python-dotenv>=1.0.0

# MLflow LLMOps tracing + experiment tracking
mlflow>=2.14.0

# ASGI server (needed for streamable-http transport)
uvicorn>=0.30.0
